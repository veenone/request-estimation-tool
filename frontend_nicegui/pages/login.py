"""Login page for the NiceGUI frontend.

Note: The primary login page is defined inline in app.py.
This module is kept for backward compatibility but the main login
page in app.py is the canonical implementation with auth provider toggle.
"""
