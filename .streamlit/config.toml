[server]
headless = true
address = "0.0.0.0"
port = 8501
enableCORS = false
enableXsrfProtection = false

# HTTPS / TLS — uncomment and set paths to enable SSL.
# sslCertFile = "certs/cert.pem"
# sslKeyFile  = "certs/key.pem"

[browser]
gatherUsageStats = false
